name: Update Calendar

on:
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0  # 确保拉取完整历史

      - name: Ensure data.json exists
        run: |
          mkdir -p data
          if [ ! -f data/data.json ]; then
            echo "[]" > data/data.json  # 如果 data.json 不存在，创建一个空数组
          fi

      - name: Cache Node.js modules
        uses: actions/cache@v2
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: |
          npm ci  # 使用 npm ci 更加稳定，能保证依赖的版本准确

      - name: Run data fetch and ICS generation
        run: |
          echo "Fetching data..."
          node scripts/fetch-data.js  # 运行数据抓取脚本
          echo "Generating ICS file..."
          node scripts/generate-ics.js  # 生成 .ics 文件

      - name: Check for changes
        id: check_changes
        run: |
          git status  # 查看更改文件
          git add .
          if git diff --cached --exit-code; then
            echo "No changes to commit"
            echo "::set-output name=changes::false"
          else
            echo "Changes detected"
            echo "::set-output name=changes::true"
          fi

      - name: Commit and push changes
        if: steps.check_changes.outputs.changes == 'true'
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "github-actions@users.noreply.github.com"
          git commit -m "Update data.json and calendar.ics"
          git push origin main